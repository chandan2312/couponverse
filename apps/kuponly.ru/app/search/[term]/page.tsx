import React from "react";
import SearchPage from "@repo/ui/pages/SearchPage";

const SearchPageApp = ({ params }: { params: any }) => {
  return <SearchPage params={params} />;
};

export default SearchPageApp;
